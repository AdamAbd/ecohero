import 'package:flutter/material.dart';

class ChallengeCreatePage extends StatelessWidget {
  const ChallengeCreatePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Membuat Kompetisi")),
      body: Column(
        children: [],
      ),
    );
  }
}
